//creating an object with properties and their values
var table = {
    legs: 3,
    color: "brown",
    priceUSD: 100,
}

// to access table object I can console.log

console.log(table);
//display the object in the developer console

// the returned value is the entire table object
// {legs: 3, color: 'brown', priceUSD: 100}

// Additionally, I can console log any individual 
// property, like this:  
console.log(table.color); // 'brown'
